[project]
name = "ccusr-supply-command"
version = "0.1.0"
description = "多业态保供指挥后端：带版本快照、窗口指标、双时态时间线、告警工作流与处置单核对"
requires-python = ">=3.11"
dependencies = []

[tool.unittest]
start-directory = "tests"

